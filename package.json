{
  "name": "aurenyx-obsidian",
  "version": "1.0.5",
  "scripts": {
    "version": "node version-bump.mjs && git add manifest.json versions.json",
    "lint": "stylelint \"**/*.css\" --max-warnings 0",
    "validate": "node scripts/validate-theme.mjs",
    "check": "npm run lint && npm run validate"
  },
  "devDependencies": {
    "stylelint": "^17.15.0",
    "stylelint-config-obsidianmd": "^0.1.0"
  },
  "private": true
}
